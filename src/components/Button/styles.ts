import {StyleSheet} from 'react-native';

const imgSize = 25;

const styles = StyleSheet.create({
  container: {
    flex: 1,
    position: 'relative',
  },
  img: {
    width: imgSize,
    height: imgSize,
  },
});

export default styles;
